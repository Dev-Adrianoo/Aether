# Aether — dependências de produção

# Áudio (captura e reconhecimento)
sounddevice>=0.4.6
numpy>=1.24.0
SpeechRecognition>=3.10.0

# TTS e playback
edge-tts>=6.1.9
pydub>=0.25.1
playsound==1.2.2

# Visão
mss>=9.0.1
Pillow>=10.0.0
pytesseract>=0.3.10  # requer tesseract-ocr instalado no sistema

# Integração e utilitários
aiohttp>=3.9.0
python-dotenv>=1.0.0

# gRPC — integração OpenClaude headless
grpcio>=1.62.0
grpcio-tools>=1.62.0
